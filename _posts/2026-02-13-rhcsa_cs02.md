---
title: RHCSA CS02 - nmcli
author: Me
date: 2026-02-13
categories:
  - 취업준비
  - 리눅스
published: true
---

## 덤프 문제

```
YUM 기본 저장소를 아래와 같이 변경하세요.

아래와 같은 YUM 저장소가 사용가능합니다.

- http://content/rhel9.0/x86_64/dvd/BaseOS
- http://content/rhel9.0/x86_64/dvd/Appstream
```

## 풀이
```
# 1. 임의의 설정 파일을 생성
vi /etc/yum.repos.d/base.repo

# 2. /etc/yum.repos.d/base.repo 안에 작성
[BaseOS]
name=BaseOS Repository
baseurl=http://content/rhel9.0/x86_64/dvd/BaseOS
enabled=1
gpgcheck=0

[AppStream]
name=AppStream Repository
baseurl=http://content/rhel9.0/x86_64/dvd/Appstream
enabled=1
gpgcheck=0

# 3. :wq로 저장하고 나옴.

# 4. dnf 캐시 날리고 반영됐는지 확인
dnf clean all
dnf repolist

```

- \[ID]: 중복되지 않는 고유 ID. 대괄호 필수.
- name: 식별자
- baseurl: url 주소
- enabled: 활성화 여부 (기본값이 1이지만 명시적으로 적는 게 좋은듯)
- gpgcheck: 0이면 서명 체크 하지 않음. 시험 환경에선 보통 0으로 설정한다고 함. (기본값은 시스템 설정에 따라 다르기 때문에, 만약 시스템 설정상 1로 풀이된다면 이후 패키지 설치 절차에서 GPG키를 요구하게 되어 난감해짐. 그냥 0으로 설정하자.)
- 위의 항목들을 꼭 외워두자!!


---

## 여담

### 만약 dnf repolist에서 오류가 난다면??

네트워크(ip, gateway, dns) 설정이 제대로 안 잡혀있으면 오류가 난다고 하니 이쪽을 확인해볼 것.

### dnf repolist -v
진짜 저장소와 연결이 되는지 확인하려면 이 명령어로 확인하면 된다고 한다.
연결된 저장소들에서 metadata를 쫙 읽어오는 과정 같다.

근데 이게 덤프문제라 그런지 제시된 url로 접속도 되지 않았고, dnf repolist -v도 당연히 오류가 났다.
저장소 중 하나라도 막히면 metadata 받아오다가 멈춰버리니..
안 쓰는 저장소는 이름을 .repo로 끝나지 않게 바꿔버리거나 enabled=0으로 바꿔놓으라고 한다.


```bash
rename .repo .repo.bak *.repo
# 해석: *.repo에 해당하는 파일들 중에서 ".repo" 라는 글자를 찾아 전부 ".repo.bak"으로 바꿔라.
```

### dnf config-manager

설정 파일을 바로 바꿔버리는 방법도 있지만
`dnf config-manager --add-repo \<url>`을 사용하는 방법도 있다.
다만 오타가 날 수 있고 enabled나 gpgcheck 등의 추가 설정을 하려면 점점 명령어가 길고 복잡해지기 때문에 그냥 파일 하나로 끝내는 게 제일 간편하다고 한다.
또한 이렇게 명령어로 만들면 .repo 파일이 분산되어 계속 추가되기 때문에 파일 관리가 귀찮다고 함.
